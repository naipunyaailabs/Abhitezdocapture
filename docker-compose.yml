services:
  api:
    build: .
    ports:
      - "${PORT:-5000}:5000"
    env_file:
      - .env
    environment:
      - PORT=5000
    volumes:
      - ./app:/app/app
      - ./main.py:/app/main.py
    restart: always

networks:
  default:
    name: docapture-network
